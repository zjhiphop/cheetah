<label class="ets-{{type}}-b" for="{{_id}}"> 
    <span class="ets-{{type}}-b-wrapper">
      <span>
        <input name="{{_name}}" id="{{_id}}" type="{{type}}"/>
        {{content}}
      </span>
    </span> 
</label>